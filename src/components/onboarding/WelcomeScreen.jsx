import WelcomeFormPage from './WelcomeFormPage';

const WelcomeScreen = ({ onComplete }) => {
    return (
        <WelcomeFormPage onComplete={onComplete} />
    );
};

export default WelcomeScreen;
